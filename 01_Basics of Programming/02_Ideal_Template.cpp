//This is ideal template for the code in cpp which include all the libraries at once and using namespace you can deduct the frequent use of std::

#include<bits/stdc++.h>//Including all the libraries may take large amount of time than inclding a specific library
using namespace std;

int main(){
  cout<<"sahil omanwar"<<endl;//No frequent usage of "std::"  as we have already included using namespace std
  cout<<"This is the initial template code "<<"\n";
}